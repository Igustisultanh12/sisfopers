# Rencana Kerja
## tentang IMPLEMENTASI KEPUTUSAN DOMAIN AKUN, PENDIDIKAN, REPOSITORI, OCR, DAN FORMAT NAMA

Status: Aktif
Tanggal dibuka: 13 Juli 2026
Identitas dokumen: 2026.07.13 Rencana Kerja tentang Implementasi Keputusan Domain Akun, Pendidikan, Repositori, OCR, dan Format Nama
Jenis dokumen: Rencana Kerja
Domain: kctrimatra
Topik: eksekusi implementasi Kep.007 (Repositori), Kep.008 (Pendidikan), Kep.009 (Akun/Login), Kep.010 (OCR), Kep.011 (Format Nama)
Keputusan terkait: `2026.Kep.007`, `2026.Kep.008`, `2026.Kep.009`, `2026.Kep.010`, `2026.Kep.011`
Mengubah: -
Digantikan oleh: -

## Instruksi Baca Wajib
- Stack: Laravel 13, PHP 8.4+; verifikasi lokal saat ini memakai **PHP 8.5.8 Laragon** di `C:\laragon\bin\php\php-8.5.8-nts-Win32-vs17-x64\php.exe`.
- Baca urutan: `AGENTS.md` -> `docs/README.md` -> `docs/CHANGELOG.md` -> workplan ini -> keputusan Kep.007-011 -> `docs/02 Keputusan/README.md`.
- **Peringatan timezone:** `date` bash di host ini tidak menghitung TZ (tzdata inactive, `date +%z`=+0000). WIB = UTC+7 hitung manual. Jangan percaya timestamp WIB dari `date` mentah.
- Verifikasi minimum: `php -l` untuk PHP, `php artisan route:list`, `php artisan test`, dan `npm run build` bila tidak membutuhkan DB MySQL aktif; `php artisan migrate:fresh --seed` ditunda sampai user memutuskan database lokal/fresh install.
- Workplan ini **hanya menurunkan** keputusan; tidak boleh mengubah tafsir Kep.007-011.

## Tujuan
Mengeksekusi seluruh acceptance criteria yang belum tercentang di Kep.007–011 (semua berstatus implementasi, bukan normatif):
1. Kep.007 → disk `private` untuk dokumen pribadi, route berproteksi + `response()->file()`, `document_access_logs`, enkripsi at rest.
2. Kep.008 → tabel `riwayat_pendidikan`, controller CRUD, soft-delete, unique constraint, kontrak parse dari Kep.010.
3. Kep.009 → aktifkan KYC (rename `EnsureFaceVerified`→`EnsureKycSubmitted`), pesan "login menggunakan NIK"→NIKC/username, migration `is_komcad`/`nrp`/`two_factor_*`/`nikc` nullable, seeder Super Admin pertama.
4. Kep.010 → runtime OCR/PDF parser + queue import SK (kode dasar siap; runtime binary eksternal wajib tersedia).
5. Kep.011 → accessor `display_name` (prefix + front_title + full_name + suffix).

## Kondisi Stop
Berhenti & lapor jika: keputusan sumber tidak ditemukan, workplan bertentangan kondisi repo, ambiguitas ubah perilaku, verifikasi fase gagal, operasi irreversible belum dikonfirmasi, scope keluar repo, atau domain ternyata masih diskusi.

## Klasifikasi Operasi
- **Reversible:** edit migration/controller/model/view, edit dokumentasi.
- **Irreversible:** `migrate:fresh` (deploy dari nol), seeder Super Admin (tulis data), `storage:link` (tautan filesystem). Wajib konfirmasi user sebelum jalan.

## Status Eksekusi
| Fase | Status | Keputusan |
| --- | --- | --- |
| F1 Basis schema & seeder | Sebagian besar (migration personels + riwayat_pendidikan + document_access_logs + kyc_submissions ditulis; `nikc` nullable; migration transisional `drop_phone_number` lama sudah dibersihkan dari fresh-install; seeder Super Admin Komcad penuh `mdwikiar` disiapkan, dengan catatan NIK sipil masih placeholder menunggu data sumber user) | Kep.008/009 |
| F2 Repositori & keamanan | Selesai kode dasar (disk `private`, upload KTP/photo/SK/kyc, route download protected, access log, cast enkripsi path, link UI utama+verifikasi/edit ke route private; route vendor direct-serve dimatikan) | Kep.007 |
| F3 Akun/Login/KYC | Selesai kode dasar (rename KYC, relasi, login NIKC/username, registrasi `username=nikc`, pesan, route personel.kyc + middleware aktif, gate `submitted_at`) | Kep.009 |
| F4 Pendidikan & gelar | Selesai kode dasar (tabel + model + relasi + CRUD dasar + verifikasi admin/koordinator/wakil berbasis grup ADA; QA DB fresh install menunggu database) | Kep.008/011 |
| F5 OCR & import SK | Sebagian besar kode dasar (structured import SK manual hidup; service Poppler/Tesseract + job queue + tombol import PDF admin sudah disiapkan; OCR KTP/ijazah/sertifikat diantrikan ke job dokumen; runtime lokal baru mendeteksi `pdftotext`, QA end-to-end menunggu DB/queue/sampel dan binary OCR lengkap) | Kep.010 |
| F6 Display nama | Selesai (accessor `display_name` + appends, lulus php -l) | Kep.011 |
| F7 Drift multi-grup lintas modul | Selesai kode dasar (koordinator/wakil wajib Komcad+Perwira, lookup kandidat/validasi penunjukan membaca role efektif primer+pivot, broadcast grup membaca pivot hak, dashboard koordinator scoped ke grup yang dipimpin, pivot hak wajib SK, MFA route dibuat dormant/tak reachable) | Kep.006/009 + 2026.11.004 |

## F1. Basis Schema & Seeder
- **Tujuan:** tulis ulang migration `personels` (self-contained fresh-install) agar sudah berisi `is_komcad`(bool,default true), `nrp`(nullable), `nikc`(nullable), `two_factor_*`(3 kolom, dormant); lahirkan migration `riwayat_pendidikan` + `dokumen_anggota`(jika perlu) + `document_access_logs`; seeder Super Admin pertama untuk Komcad penuh (`mdwikiar` / `gwganteng`).
- **Klasifikasi:** Reversible (tulis migration) + Irreversible (`migrate:fresh`, seeder → konfirmasi).
- **File yang disentuh:** `database/migrations/2026_07_06_055601_create_personels_table.php`, migration baru `riwayat_pendidikan`/`document_access_logs`, `database/seeders/DatabaseSeeder.php`/SuperAdminSeeder.
- **Verifikasi fase:** `php -l` semua file; `php artisan migrate:fresh --seed` bila DB MySQL/MariaDB fresh install sudah diputuskan.
- **Langkah jika gagal:** batalkan migration, kembalikan ke state diskusi, lapor.

## F2. Repositori & Keamanan (Kep.007)
- **Tujuan:** pindahkan upload KTP/ijazah/SK dari disk `public` ke disk `private`; route `personel.document.download` berproteksi + `response()->file()`; model `DocumentAccessLog`; enkripsi cast kolom path.
- **Klasifikasi:** Reversible.
- **File yang disentuh:** `config/filesystems.php` (disk private), `AuthController`/`SuratKeputusanController`/`MasterPersonelController` (ganti `store('...','public')`→`'private'`), route `web.php` (tambah download route), model + migration `document_access_logs`.
- **Verifikasi fase:** `php -l`; cek route terdaftar `php artisan route:list`; akses file via route (bukan URL publik).
- **Langkah jika gagal:** rollback ke disk public sementara, lapor.

## F3. Akun / Login / KYC (Kep.009)
- **Tujuan:** login identifier NIKC/username (bukan NIK); pesan approval "login menggunakan NIK"→"NIKC atau username"; aktifkan KYC (rename middleware `EnsureFaceVerified`→`EnsureKycSubmitted`, pasang di `role:personel`, route `personel.kyc` + view Swafoto); MFA route/field tetap dormant.
- **Klasifikasi:** Reversible.
- **File yang disentuh:** `AuthController`, `ApproveRegistrationAction`, `VerificationController`, `EnsureFaceVerified.php`→`EnsureKycSubmitted.php`, `bootstrap/app.php`, `routes/web.php`, view KYC.
- **Verifikasi fase:** `php -l`; `php artisan route:list` (route `personel.kyc` ada, `profile.mfa*` tidak direach).
- **Langkah jika gagal:** nonaktifkan route KYC, kembalikan ke dead-code, lapor.

## F4. Pendidikan & Gelar (Kep.008 / Kep.011)
- **Tujuan:** `EducationController` + route CRUD (personel input, koordinator verify); `riwayat_pendidikan` isi dari upload ijazah & import SK (Kep.010); accessor `display_name` (Kep.011).
- **Klasifikasi:** Reversible.
- **File yang disentuh:** `app/Http/Controllers/Education/`, `routes/web.php` (grup personel), `Personel.php` (accessor `display_name`), `riwayat_pendidikan` migration, view riwayat.
- **Verifikasi fase:** `php -l`; `php artisan route:list`; unit test accessor format nama.
- **Langkah jika gagal:** batalkan controller, lapor.

## F5. OCR & Import SK (Kep.010)
- **Tujuan:** pilih library OCR (gambar KTP/KTA/Ijazah) + PDF text parser; queue job per PDF SK; dedupe by `nomor_surat`; parse kolom → `riwayat_pendidikan` (kontrak Kep.008 T8).
- **Klasifikasi:** Reversible (kode) + Irreversible (`php artisan queue:work` consume job).
- **File yang disentuh:** `config/ocr.php`, `app/Services/Ocr/*`, Job `ImportSuratKeputusanArtifactJob`, Job `ExtractDocumentOcrJob`, `SuratKeputusanController`, `EducationController`, `AuthController`, route import SK, UI detail SK/verifikasi/pendidikan, migration artefak SK/personel/riwayat pendidikan.
- **Verifikasi fase:** `composer validate`; `php -l`; dry-run parse sampel SK (arsip halaman 4 KEP/1717).
- **Langkah jika gagal:** tahan integrasi, kembalikan ke manual upload, lapor.

## F6. Display Nama (Kep.011)
- **Tujuan:** accessor `display_name` = `PREFIX_PANGKAT [FRONT_TITLE] + full_name + SUFFIX_GELAR`; reuse `display_pangkat`; suffix dari `riwayat_pendidikan` AKADEMIK.
- **Klasifikasi:** Reversible.
- **File yang disentuh:** `Personel.php` (accessor), view yang menampilkan nama.
- **Verifikasi fase:** `php -l`; test string `Letda KC (W) dr. Budi Santoso, S.Psi., M.Hum.`.
- **Langkah jika gagal:** fallback ke `display_pangkat + full_name`, lapor.

## Verifikasi Final
- [ ] F1: `migrate:fresh --seed` lulus, Super Admin bisa login
- [ ] F2: dokumen tak ter-URL publik; akses via route terproteksi + tercatat log
- [ ] F3: login NIKC/username; KYC aktif; pesan benar; MFA dormant
- [ ] F4: CRUD pendidikan jalan; koordinator hanya verify; suffix gelar ter-compute
- [ ] F5: OCR/import SK jalan end-to-end setelah DB/queue aktif dan tersedia sampel PDF SK untuk dry-run
- [ ] F6: `display_name` benar formatnya

## Hasil Verifikasi Lokal Terkini
- Verifikasi runtime tanpa DB: PHP 8.5.8 berhasil boot-test; `php artisan route:list` lulus dan menampilkan 76 route aktif setelah MFA route dibuat dormant/tak reachable.
- Verifikasi sintaks: `php -l` lulus untuk controller/model/migration/config yang terkait F1-F4/F6.
- Verifikasi frontend: `npm run build` lulus.
- Verifikasi DB penuh (`migrate:fresh --seed`/`migrate:status`) sengaja ditunda karena user belum memutuskan instalasi DB lokal; target tetap fresh install setelah DB dibuat.
- Verifikasi test: `php artisan test` lulus (25 tests, 13 passed, 12 skipped, 35 assertions). Skip bersifat eksplisit untuk route auth bawaan Breeze yang belum menjadi fitur aktif (`password reset`, `password confirmation`, `email verification`, `hapus akun mandiri`).
- Verifikasi OCR health: service Laravel mendeteksi `pdftotext`; `pdftoppm` dan `tesseract` belum tersedia sebagai runtime aplikasi stabil.
- Sisa utama: QA OCR end-to-end menunggu DB/queue aktif, sampel PDF/dokumen, serta binary `pdftoppm` + `tesseract`; structured import SK manual dan kode dasar import PDF/OCR sudah hidup.

## Hasil Verifikasi Lokal Lama
- Verifikasi runtime (`migrate:fresh`/`test`) **belum bisa** karena env Laragon + MySQL MATI DAN Laravel 13 menuntut PHP ≥8.4.1 (Laragon: 8.3.30 + 8.5.x dihindari). Verifikasi yang tersedia: `php -l` (PHP 8.3.30) lulus untuk semua file PHP/Migration yang disentuh.
- **Yang SUDAH dikerjakan & lulus php -l (commit ca43e0e + berikutnya):**
  - F6: accessor `display_name` (+ `$appends`).
  - F1: migration `personels` punya `two_factor_*`/`is_komcad`/`nrp`; `nikc` nullable; tabel `riwayat_pendidikan`, `document_access_logs`, dan `kyc_submissions` ada. Seeder Super Admin Komcad penuh `mdwikiar` sudah disiapkan.
  - F3: rename KYC, relasi `kycSubmission()`, login `nikc`/`username`, registrasi `username=nikc`, pesan approval, route `personel.kyc` + middleware `kyc` AKTIF, gate memakai `submitted_at`.
  - F2: disk `private` DITAMBAH di `filesystems.php`; upload KTP/photo/SK/kyc → `private` (AuthController, SuratKeputusanController, MasterPersonelController, KycSubmissionController).
- **BLOCKER / SISA (butuh env + konfirmasi):**
  - F2 lanjut: enkripsi kolom path sensitif + wiring UI/link download dokumen BELUM.
  - F4: `EducationController` CRUD BELUM (tabel + model + relasi sudah ada).
  - F5: OCR lib + queue BELUM.
  - F1: seeder Super Admin Komcad penuh sudah disiapkan.

## Temuan Pasca-Finalisasi (arsip — STATUS: SUDAH DIPERBAIKI)
Ditemukan setelah commit `cb047eb`. Perbaikan dilakukan di commit `a71a8ea` (preamble + Digantikan oleh + 4 dari 5 changelog) dan commit berikutnya (2 changelog tersisa yang masih swapped).

1. **Metadata arsip eksplisit** → SUDAH: preamble `Status Arsip/Selesai pada 2026-07-13 10:32:57 WIB/Rujukan keputusan` ditambah kelima arsip.
2. **`Digantikan oleh: -`** → SUDAH: diisi Kep.007/008/009/010/011.
3. **Changelog salah nomor** → SUDAH (lewati 2 revisi): akhirnya Akun→Kep.009, Repositori→Kep.007, Riwayat Pendidikan→Kep.008 benar sejak `a71a8ea`; OCR→Kep.010 & Format Nama→Kep.011 dikoreksi di commit ini (sebelumnya ke-swap di `a71a8ea`).
4. **Rencana perbaikan arsip** → SELESAI.

## Bug Ditemukan & Diperbaiki (implementasi, commit ini)
- **F3 bug kritis:** `bootstrap/app.php` pakai `EnsureKycSubmitted::class` tanpa `use` import → TAMBAH import.
- **F3 bug:** `EnsureKycSubmitted` panggil `$user->kycSubmission` tapi relasi belum ada → TAMBAH `kycSubmission()` di `Personel.php` (reuse `FaceVerification` sementara, rename ke `kyc_submissions` ditunda ke F1).
- **F3 (Kep.009):** login `AuthController` cari `nik`/`nikc` → ubah ke `nikc`/`username` saja.
- **F3 (Kep.009):** pesan approval `ApproveRegistrationAction` & `VerificationController` "login menggunakan NIK" → "NIKC atau username".
- **F2 (Kep.007):** upload KTP/photo/SK `AuthController` disk `public` → `private` (route download protected + `document_access_logs` masih BUTUH F2 tindak lanjut).
- Catatan: 2 file (ApproveRegistrationAction/VerificationController) ke-rewrite line-ending CRLF→LF oleh git (cosmetic, tak ubah logika).

## Audit Ulang & Perbaikan Temuan (turn ini)
- **F3 route bug:** `routes/web.php` memakai `KycSubmissionController::class` tanpa import. Perbaikan: tambah `use App\Http\Controllers\Personel\KycSubmissionController;`.
- **F3 gate bug:** middleware KYC mengecek `kyc_verified_at`, sedangkan controller hanya mengisi `submitted_at`. Perbaikan: `EnsureKycSubmitted` sekarang mengecek `submitted_at`, sesuai KYC Pengajuan = bukti submit, bukan face recognition/self-verify.
- **F3 UI drift:** label login masih `Username / NIK`. Perbaikan: label dan placeholder menjadi `Username / NIKC`.
- **F1 schema drift:** `personels.nikc` masih wajib. Perbaikan: migration fresh-install mengubah `nikc` menjadi nullable untuk Super Admin Komcad penuh dan membuka jalur identifier sesuai keputusan aktif.
- **F1 model drift:** `is_komcad`/`nrp` sudah ada di schema tetapi belum di model. Perbaikan: tambah ke `$fillable`, dan `is_komcad` ke `$casts`.
- **F4 relasi drift:** `display_name` memakai `riwayatPendidikan()`, tetapi relasi/model belum ada. Perbaikan: tambah model `RiwayatPendidikan` dan relasi `Personel::riwayatPendidikan()`.
- **F2 route/log gap:** belum ada jalur unduh private berproteksi yang mencatat akses. Perbaikan: tambah `DocumentController`, model `DocumentAccessLog`, dan route `personel.document.download` dengan `response()->file()` + pencatatan `document_access_logs`.
- **Status sisa:** F2 hardening otorisasi granular masih bisa diperluas; F4 sudah memiliki CRUD dasar namun masih perlu QA runtime; F5 butuh instalasi/konfigurasi binary OCR eksternal untuk QA end-to-end; F1 seeder Super Admin Komcad penuh sudah disiapkan dan tinggal divalidasi runtime.
- **Verifikasi:** `php -l` lulus untuk `routes/web.php`, `EnsureKycSubmitted.php`, `Personel.php`, `RiwayatPendidikan.php`, `DocumentAccessLog.php`, `DocumentController.php`, dan migration personels. `php artisan route:list`/`migrate:fresh` masih belum bisa diverifikasi karena Composer vendor menuntut PHP >=8.4.1 sementara PHP Laragon yang tersedia 8.3.30.

## Implementasi Lanjutan Setelah Arahan Seeder
- **Arahan user:** Super Admin dapat berasal dari anggota Komcad penuh. Dampak: seeder `mdwikiar` diperlakukan sebagai Super Admin Komcad penuh, bukan akun non-Komcad terpisah.
- **F2 repositori:** tambah cast `encrypted` untuk path dokumen sensitif (`photo_profile`, `ktp_document`, `file_ijazah_path`, `swafoto_path`, `artifact_file_path`, path ijazah/sertifikat riwayat pendidikan), dan wiring UI utama diarahkan ke route private `personel.document.download` alih-alih URL `/storage`.
- **F4 pendidikan:** tambah `EducationController`, route CRUD personel, route input/admin view, route verifikasi admin/koordinator, model `RiwayatPendidikan`, dan halaman riwayat pendidikan personel.
- **F5 import SK manual:** format entri lampiran SK diperluas menjadi `NIKC|Nama Lengkap|Pangkat Tujuan|Pendidikan`; saat `basis=penyesuaian_ijazah`, proses pangkat membuat/memperbarui `riwayat_pendidikan` terverifikasi dari data lampiran SK.
- **Drift yang diperbaiki:** softcopy SK di halaman detail admin tidak lagi memakai `/storage/{path}`, tetapi memakai route private berproteksi agar selaras dengan Kep.007.
- **Drift lifecycle Kep.007:** status artefak SK lama `draft/siap_digunakan` diganti ke lifecycle keputusan `PENDING_TMP/AKTIF/DITOLAK/KADALUARSA/SOFT_DELETED`; unggahan personel menjadi `PENDING_TMP`, pencatatan admin menjadi `AKTIF`, dan selector UI/filter memakai status baru.
- **Status QA:** `php -l` lulus untuk seluruh PHP/controller/model/migration/seeder yang berubah dan `npm run build` lulus. `php artisan route:list` masih gagal sebelum bootstrap karena Composer menuntut PHP >=8.4.1 sementara binary Laragon yang tersedia 8.3.30.

## Audit Final Implementasi & Perbaikan Temuan (turn ini)
- **F2 bug runtime disk private:** konfigurasi `private` sempat melahirkan route serve otomatis `/private/{path}` yang tidak mencatat `document_access_logs`. Perbaikan: disk `private` tidak lagi `serve` langsung; akses dokumen sensitif wajib lewat `personel.document.download`.
- **F2 drift UI:** halaman verifikasi admin dan edit personel masih memakai `/storage/{path}` untuk foto/SK private. Perbaikan: UI diarahkan ke helper `documentUrl()` yang memakai route private berproteksi.
- **F2/F4 bug path terenkripsi:** scope koordinator mencari `file_ijazah_path`/`file_sertifikat_path` dengan query plaintext, padahal kolom dicast `encrypted`. Perbaikan: lookup evidence pendidikan membandingkan nilai setelah model mendekripsi cast.
- **F4 bug scope wakil koordinator:** verifikasi dan akses evidence hanya memeriksa `koordinator_personel_id`. Perbaikan: scope kini mencakup `wakil_koordinator_personel_id`.
- **F1/F4 migration test drift:** migration drop `personels.phone_number` gagal di SQLite test karena index masih ada. Perbaikan: drop index sebelum drop column agar test suite tidak berhenti di migration tersebut.
- **F3/test drift auth model:** test scaffold masih memakai `App\Models\User`, field `email` untuk login, route `/profile`, dan asumsi registrasi langsung authenticated. Perbaikan: tambah `PersonelFactory`, arahkan test ke model auth `Personel`, login `username`, route `/account/settings`, registrasi antrean verifikasi, dan data referensi minimal pangkat/provinsi.
- **F3/test drift fitur belum aktif:** test Breeze untuk reset password, konfirmasi password, verifikasi email, dan hapus akun mandiri tidak sesuai route/keputusan aktif saat ini. Perbaikan: test tersebut diberi skip eksplisit agar tidak dibaca sebagai bug runtime.
- **QA terbaru:** PHP 8.5.8 tersedia untuk boot-test; `php artisan route:list` lulus (76 route aktif setelah MFA route dibuat dormant/tak reachable), `php -l` lulus untuk file terkait, `php artisan test` lulus (25 tests, 13 passed, 12 skipped), `npm run build` lulus, dan wrapper Composer repo lulus `validate`. Verifikasi DB penuh sengaja ditunda karena user belum memutuskan instalasi DB lokal; target tetap fresh install saat DB dibuat.
- **F5 OCR/import SK:** structured import SK manual (`NIKC|Nama|Pangkat|Pendidikan`) sudah hidup dan dapat menurunkan `riwayat_pendidikan`; kode dasar OCR/import juga sudah disiapkan tanpa Composer tambahan melalui service Poppler/Tesseract, job queue `ImportSuratKeputusanArtifactJob`, status import di artefak SK, dan tombol admin `Ekstrak dari PDF`. OCR gambar/PDF untuk KTP/ijazah/sertifikat juga diantrikan via `ExtractDocumentOcrJob`, menyimpan hasil teks private + confidence + error tanpa auto-otoritas. Health lokal mendeteksi `pdftotext`, tetapi `pdftoppm` dan `tesseract` belum tersedia sebagai runtime aplikasi stabil; QA OCR end-to-end tetap menunggu DB/queue aktif, sampel PDF/dokumen, dan binary lengkap.

## Audit Lanjutan Setelah Aktivasi Composer/OCR (turn ini)
- **Temuan D1 - drift QA workplan:** workplan sempat mencatat `route:list` 77 route, sedangkan saat route import OCR SK ditambahkan route aktual menjadi 78. Catatan historis ini kemudian ditimpa oleh audit L6: setelah MFA route dibuat dormant/tak reachable, route aktif terkini menjadi 76.
- **Temuan D2 - drift file F5:** workplan F5 masih menyebut penambahan library via `composer.json`, padahal implementasi aktual memakai binary Poppler/Tesseract via wrapper service tanpa package Composer baru. Perbaikan: daftar file F5 diselaraskan ke `config/ocr.php`, service OCR, job queue, controller, route, UI, dan migration.
- **Temuan B1 - risiko kehilangan entri SK:** job OCR sebelumnya menghapus semua `personelEntries()` walaupun hasil parse kosong. Perbaikan: bila parse kosong, entri lama dipertahankan, hasil teks tetap disimpan, status import menjadi `FAILED`, dan error menjelaskan penyebabnya.
- **Temuan B2 - hasil teks OCR belum dapat diaudit:** `extracted_text_path` sudah disimpan tetapi belum ditautkan di UI. Perbaikan: detail SK kini menampilkan tautan `Buka teks` lewat route private berlog; `DocumentController` mengizinkan akses `extracted_text_path` untuk admin dan personel yang terkait entri SK.
- **Temuan B3 - job pasti gagal saat `pdftotext` tidak tersedia:** UI tombol import sebelumnya hanya bergantung pada ada/tidaknya PDF. Perbaikan: tombol `Ekstrak dari PDF` disabled bila `pdftotext` tidak tersedia.
- **Temuan B4 - cross-validation NIKC belum tercatat:** import/manual match kini menambahkan catatan cross-validation dasar bulan/tahun lahir dan kode provinsi NIKC terhadap profil personel sebagai flag review admin.
- **Temuan B5 - hardening path dokumen private:** route download private sudah berizin dan berlog, tetapi belum menolak segmen traversal/path absolut secara eksplisit sebelum akses storage. Perbaikan: `DocumentController` menormalisasi separator dan menolak path kosong, `..`, serta drive path Windows.
- **Temuan D3 - OCR gambar MVP belum terwiring:** service OCR sudah bisa membaca gambar, tetapi upload KTP/ijazah/sertifikat belum mengantrikan OCR dan hasilnya belum terlihat di verifikasi admin. Perbaikan: tambah metadata OCR pada `personels`/`riwayat_pendidikan`, job `ExtractDocumentOcrJob`, dispatch dari registrasi dan riwayat pendidikan, akses hasil teks via route private, serta status OCR di UI verifikasi/pendidikan.
- **Status runtime OCR lokal:** service Laravel kini mendeteksi `pdftotext` melalui konfigurasi/candidate binary Laragon; `pdftoppm` dan `tesseract` belum tersedia sebagai runtime aplikasi stabil. Karena user menunda instalasi DB lokal, OCR end-to-end tetap menunggu DB/queue aktif, sampel PDF/dokumen, dan binary lengkap, bukan perubahan kode utama.

## Audit Multi-Grup & Metadata (2026-07-13 22:34 WIB)
- **Temuan M1 - drift metadata Kep.008:** metadata `Turunan dari` masih menunjuk Kep.006, padahal keputusan ini lahir dari arsip diskusi riwayat pendidikan. Perbaikan: `Turunan dari` diarahkan ke arsip `[SELESAI] 2026.07.03 Diskusi tentang Riwayat Pendidikan dan Diklat Anggota`.
- **Temuan M2 - drift metadata arsip UI:** arsip diskusi UI sudah punya preamble `Keputusan final yang dihasilkan`, tetapi metadata badan masih menunjuk Kep.001 dan `Digantikan oleh: -`. Perbaikan: metadata diarahkan ke `docs/02 Keputusan/2026.02.002 tentang Standar UI dan Design System Kctrimatra.md`.
- **Temuan M3 - multi-grup belum penuh:** repo masih dominan `personels.grup_angkatan_id` + history, belum ada pivot hak aktif sesuai `docs/11 Referensi Teknis/2026.11.004 tentang Turunan Operasional Hak Multi-Grup Angkatan.md`. Perbaikan: tambah tabel `personel_grup_angkatan_hak`, model `PersonelGrupAngkatanHak`, relasi `Personel::grupAngkatans()`/`GrupAngkatan::personels()`, dan helper `Personel::grantGrupAngkatanHak()`.
- **Temuan M4 - fresh install fase 1 belum seed pivot:** seeder membuat grup, admin, koordinator, wakil, dan history, tetapi belum menulis hak pivot. Perbaikan: seeder mengisi `personel_grup_angkatan_hak` untuk akun contoh Komcad yang sudah ada dan menautkan `mdwikiar` sebagai Super Admin Komcad penuh.
- **Temuan M5 - alur operasional belum sinkron pivot:** registrasi, input admin, transfer anggota, dan penugasan koordinator masih hanya mengubah FK/cache dan history. Perbaikan: alur tersebut sekarang memanggil `grantGrupAngkatanHak()` agar pivot menjadi sumber relasi multi-grup aktif; `personels.grup_angkatan_id` tetap dipertahankan sebagai cache primary group untuk kompatibilitas UI lama.
- **Temuan M6 - latent bug grup tanpa SK:** `GrupAngkatan::resolveForPersonel()` masih bisa mencoba membuat grup baru tanpa `surat_keputusan_artifact_id`, bertentangan dengan migration dan keputusan bahwa grup lahir dari SK aktif. Perbaikan: resolver sekarang hanya mengembalikan grup aktif yang sudah ada; bila belum ada, ia menolak dan meminta grup dibuat dari SK aktif terlebih dahulu.
- **Catatan batas:** constraint "hanya satu primary per personel" ditegakkan di model sebelum simpan agar tetap portable di MySQL/MariaDB dan SQLite test. Jika kelak perlu hard constraint database, gunakan generated column/partial-index sesuai engine target setelah DB final dipilih.

## Audit Perbaikan Drift Lanjutan (2026-07-13 23:02 WIB)
- **Temuan L1 - validasi koordinator/wakil belum lengkap:** penunjukan koordinator/wakil masih hanya memeriksa role, belum memeriksa `is_komcad=true` dan pangkat Perwira sesuai `2026.11.004`. Perbaikan: opsi pilihan dan validasi `GrupAngkatanController` kini hanya menerima personel Komcad berpangkat Perwira.
- **Temuan L2 - seeder default melanggar syarat Perwira:** akun contoh koordinator/wakil masih berpangkat `Prada KC`. Perbaikan: koordinator dan wakil default diselaraskan ke `Letda KC` serta NIKC contoh berawalan kode Perwira/Matra Darat.
- **Temuan L3 - broadcast grup masih single-FK:** target broadcast `GRUP_ANGKATAN` masih memakai `personels.grup_angkatan_id`, sehingga hak sekunder multi-grup tidak ikut menerima broadcast. Perbaikan: filter broadcast grup kini memakai relasi pivot `Personel::grupAngkatans()`.
- **Temuan L4 - dashboard koordinator terlalu global:** statistik koordinator masih menghitung seluruh personel, bukan personel pada grup yang dipimpin. Perbaikan: statistik `total_personel` dan `personel_aktif` kini scoped ke grup yang dikoordinasi/wakili melalui pivot hak multi-grup.
- **Temuan L5 - dasar SK pivot masih nullable:** migration pivot hak multi-grup masih mengizinkan `surat_keputusan_artifact_id = null`, padahal hak grup wajib berbasis SK aktif. Perbaikan: migration menjadikan FK SK wajib dan model `PersonelGrupAngkatanHak` menolak simpan tanpa dasar SK.
- **Temuan L6 - MFA belum benar-benar dormant:** route MFA masih terdaftar walau controller menolak aktivasi. Perbaikan: route MFA dikomentari sehingga field/controller tetap sebagai artefak dormant, tetapi endpoint tidak reachable sampai ada keputusan baru.
- **Catatan QA:** setelah MFA route dibuat dormant, jumlah route aktif turun dari 78 menjadi 76. Angka QA terbaru harus memakai hasil verifikasi setelah patch ini, bukan catatan lama.

## Audit Perbaikan Drift Role-Hak & Fresh Install (2026-07-14 WIB)
- **Temuan R1 - role multi-hak belum terbaca penuh di modul grup:** kandidat dan validasi koordinator/wakil masih memeriksa `role` primer. Perbaikan: `Personel` memiliki scope `whereHasEffectiveRole()`, lalu `GrupAngkatanController` dialihkan membaca role efektif primer + `personel_role_haks`.
- **Temuan R2 - broadcast masih membaca role lama:** target broadcast `ALL/MATRA/TAHUN/GRUP` sebelumnya menyaring `role=personel` primer saja. Perbaikan: `BroadcastController` kini memakai role efektif sehingga transisi ke model hak baru tidak lagi drift di query target.
- **Temuan R3 - registrasi dan input admin belum menulis pivot role-hak primer:** akun baru memang punya `role_id`, tetapi belum selalu punya entri `personel_role_haks`. Perbaikan: `AuthController` dan `MasterPersonelController` kini langsung memanggil `grantRoleHak(..., true)` setelah membuat personel.
- **Temuan R4 - urutan gelar akademik berisiko salah:** accessor `display_name` dulu mengurutkan `jenjang` alfabetis. Perbaikan: urutan gelar kini eksplisit (`Profesi`, `D3`, `D4`, `S1`, `S2`, `S3`) agar prefix/suffix lebih stabil sesuai Kep.011.
- **Temuan R5 - migration fresh-install masih menyisakan file transisi no-op:** `2026_07_12_000001_drop_phone_number_from_personels_table.php` tidak lagi relevan setelah kolom `phone_number` dicabut dari migration inti. Perbaikan: file migration transisional dihapus agar fase fresh-install lebih bersih dan tidak membingungkan audit.
- **Temuan R6 - seeder role belum aman untuk dijalankan ulang dan hak efektif anggota Komcad belum lengkap:** role masih `insertGetId` dan akun Komcad yang memegang hak admin/koordinator belum otomatis punya hak `personel`. Perbaikan: seeder role diubah ke `upsert`, seed `personel_role_haks` ditambah untuk `mdwikiar`, `koordinator.kc`, dan `wakil.koordinator.kc` agar identitas anggota Komcad tetap tercermin walau memegang hak organisasi lain.
- **Sisa jujur:** NIK sipil Super Admin `mdwikiar` tetap placeholder `0000000000000001` karena data sumber belum diberikan user; tidak aman untuk diarang. QA `migrate:fresh --seed` tetap menunggu database fresh install saat user siap.

## Audit Penuh Keputusan vs Repo (2026-07-14 WIB)
- **Temuan A1 - penunjukan koordinator/wakil belum end-to-end sesuai Kep.006:** repo sudah bisa membaca role efektif untuk kandidat, tetapi alur penunjukan masih belum menjadi sumber pemberian hak `koordinator`/`wakil koordinator` bagi personel biasa. Praktiknya, kandidat masih harus lebih dulu memiliki hak itu sebelum bisa dipilih. Status: drift implementasi masih ada.
- **Temuan A2 - model registrasi hybrid Kep.009 belum lengkap:** `nikc` masih wajib di request registrasi dan form publik belum menyediakan cabang `ijazah latsarmil` sebagai bukti ringan saat NIKC belum ada. Jalur upload SK berat ada, tetapi opsi ringan keputusan belum tercermin penuh.
- **Temuan A3 - KYC Pengajuan masih pasca-login:** route dan controller KYC sudah aktif, tetapi swafoto pengajuan belum menjadi bagian formulir registrasi awal. Implementasi saat ini masih membaca KYC sebagai gerbang setelah akun tercipta, bukan artefak registrasi.
- **Temuan A4 - fresh-install non-Komcad masih drift:** keputusan membuka `nik` nullable untuk skenario Super Admin non-Komcad, tetapi migration inti `personels.nik` masih wajib. Dampaknya, seed terpaksa memakai placeholder NIK.
- **Temuan A5 - soft-delete pendidikan berpotensi mentok oleh unique key:** `riwayat_pendidikan` memakai soft-delete, tetapi unique key belum memasukkan `deleted_at`, sehingga entri yang sudah dihapus lunak berisiko tidak bisa dibuat ulang dengan identitas sama.
- **Temuan A6 - batas akses koordinator ke dokumen masih terlalu lebar dibanding Kep.007:** koordinator/wakil saat ini dapat membuka evidence pendidikan personel grupnya selama path cocok, belum dibatasi ke dokumen yang memang sedang diverifikasi pada konteks tugas tertentu.
- **Temuan A7 - OCR Kep.010 baru parsial:** fondasi queue, OCR KTP/ijazah, dan structured import SK sudah ada, tetapi artefak/rute KTA belum tampak di repo, dan normalisasi nama untuk match OCR/SK belum benar-benar men-strip prefix pangkat + suffix gelar sesuai keputusan canonical.
- **Temuan A8 - legacy pendidikan masih tersisa di `personels`:** field `education_level`, `study_program`, dan turunannya masih hidup sebagai fallback runtime, padahal Kep.008 sudah menempatkan `riwayat_pendidikan` sebagai sumber utama. Ini belum bug fatal, tetapi masih drift transisi schema.
- **Temuan A9 - notifikasi struktural Kep.006 belum hidup:** riwayat perubahan grup/koordinator sudah tercatat, namun dispatch notifikasi struktural ke koordinator/wakil/super admin sesuai template keputusan belum tampak terimplementasi; notifikasi aktif yang jelas baru broadcast.
- **Temuan A10 - drift metadata/dokumen aktif masih ada:** nama file Kep.011 aktual adalah `2026.Kep.011 tentang Format Nama Lengkap dengan Prefix Pangkat dan Suffix Gelar.md`, sementara sebagian referensi historis masih menyebut judul lama dengan akhiran `Pendidikan`; workplan juga masih memuat snapshot hasil test lama (`25 tests`) dan karakter mojibake pada beberapa bagian.

## Penyesuaian Seeder Super Admin (2026-07-13 23:06 WIB)
- **Arahan user:** siapkan kredensial seed Super Admin `mdwikiar` / `gwganteng` untuk fresh install.
- **Perbaikan:** `DatabaseSeeder` sekarang membuat akun role `admin` default dengan username `mdwikiar`, email `mdwikiar@kctrimatra.local`, dan password hash dari `gwganteng`; lookup seeder admin juga diarahkan ke username tersebut agar rank history, grup history, nomor HP primer, pivot hak grup, dan riwayat pendidikan `S1 Psikologi` tetap terisi.
- **Catatan batas:** perubahan ini tetap memakai model `personels`/role `admin` existing sebagai representasi teknis Super Admin penuh, tetapi seed sekarang harus merefleksikan identitas Komcad Anda dan tidak lagi dibingkai sebagai akun generik.

## Catatan Perubahan Detail
- Workplan ini lahir SETELAH Kep.007–011 ditetapkan (2026-07-13). Tidak mengubah tafsir keputusan.
- Penomoran fase mengikuti urutan dependensi: schema (F1) → repositori (F2) → akun (F3) → pendidikan/gelar (F4) → OCR (F5) → display (F6).

## Status Commit Lokal
- Workplan dibuat, belum ada commit eksekusi kode (fase belum dijalankan). Commit dokumen ini dilakukan sekalian dengan finalisasi diskusi→keputusan (commit `cb047eb` sudah menutup lifecycle diskusi→keputusan; workplan ini commit terpisah).

## Catatan QA dan Penutupan
- Workplan diarsipkan `[SELESAI]` hanya bila seluruh fase `Selesai` + verifikasi final lulus + `Selesai pada` terisi WIB.
- Jika audit pasca-penutupan membuktikan belum tuntas, workplan diaktifkan ulang ke `03 Rencana Kerja/`.
